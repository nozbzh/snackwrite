%h1= "Contest number #{@contest.id}"
%h2= "Word was: #{@contest.topic.word.capitalize}"

.row
  .contest-js
    - @contest.posts.each do |post|
      = render 'posts/index', post: post

    - if @contest.finished?
      = render 'votes/vote', contest: @contest, next_contest: @next_contest
